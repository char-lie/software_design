# https://github.com/char-lie/software_design
language:
  python
python:
  2.7
addons:
  postgresql: "9.4"
services:
  - postgresql
before_script:
  - psql -f create_dbs.sql -U postgres
script:
  - ./tests.sh
after_script:
  - psql -f drop_dbs.sql -U postgres

